import { gql } from "@apollo/client";
import { useQuery } from "@apollo/client";
import Link from "next/link";

const Me = () => {
  return (
    <div>
      <h1>Your profile</h1>
    </div>
  );
};

export default Me;
