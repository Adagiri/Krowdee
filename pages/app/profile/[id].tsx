import { gql } from "@apollo/client";
import { useQuery } from "@apollo/client";
import Link from "next/link";

//other people's profile view
const Profile = () => {
  return (
    <div>
      <h1>Other persons profile view</h1>
    </div>
  );
};

export default Profile;
