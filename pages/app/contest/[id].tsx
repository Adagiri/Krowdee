import { gql } from "@apollo/client";
import { useQuery } from "@apollo/client";
import Link from "next/link";

//prep, gametime and result are all here. also, host view and players view are all here.
const contest = () => {
  return (
    <div>
      <h1>contest</h1>
    </div>
  );
};

export default contest;
